---
phase: quick-260413-e31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/legal/parse-flexible-date.ts
  - src/lib/legal/index.ts
  - src/lib/legal/calculate-deadlines.ts
  - src/lib/legal/calculate-prescription.ts
  - src/lib/legal/check-waiver-validity.ts
  - src/lib/ai/tools.ts
  - src/__tests__/parse-flexible-date.test.ts
  - src/__tests__/legal-deadlines.test.ts
  - src/__tests__/legal-prescription.test.ts
  - src/__tests__/legal-waiver.test.ts
autonomous: true
requirements: [FLEX-DATE]

must_haves:
  truths:
    - "parseFlexibleDate('2026-03-15') returns '2026-03-15'"
    - "parseFlexibleDate('03/15/2026') returns '2026-03-15'"
    - "parseFlexibleDate('March 15, 2026') returns '2026-03-15'"
    - "All three legal tools accept any of the three date formats and produce correct results"
    - "Existing tests still pass unchanged (ISO format remains valid)"
    - "Invalid date strings throw a clear error"
  artifacts:
    - path: "src/lib/legal/parse-flexible-date.ts"
      provides: "parseFlexibleDate utility function"
      exports: ["parseFlexibleDate"]
    - path: "src/__tests__/parse-flexible-date.test.ts"
      provides: "Unit tests for flexible date parsing"
      min_lines: 40
  key_links:
    - from: "src/lib/legal/calculate-deadlines.ts"
      to: "src/lib/legal/parse-flexible-date.ts"
      via: "import and call at function entry"
      pattern: "parseFlexibleDate"
    - from: "src/lib/legal/calculate-prescription.ts"
      to: "src/lib/legal/parse-flexible-date.ts"
      via: "import and call at function entry"
      pattern: "parseFlexibleDate"
    - from: "src/lib/legal/check-waiver-validity.ts"
      to: "src/lib/legal/parse-flexible-date.ts"
      via: "import and call at function entry"
      pattern: "parseFlexibleDate"
---

<objective>
Add a parseFlexibleDate() utility to the legal tools module so all three computation tools (calculateDeadlines, calculatePrescription, checkWaiverValidity) accept ISO ("2026-03-15"), US format ("03/15/2026"), and natural language ("March 15, 2026") date inputs -- not just strict ISO 8601.

Purpose: The AI model usually passes ISO dates, but conversation context may produce varied formats. Flexible parsing per CONTEXT.md decision adds resilience without adding a dependency.

Output: parseFlexibleDate.ts utility, integration into all 3 tools, tests for the parser, verification that existing tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/legal/calculate-deadlines.ts
@src/lib/legal/calculate-prescription.ts
@src/lib/legal/check-waiver-validity.ts
@src/lib/legal/index.ts
@src/lib/ai/tools.ts
@src/__tests__/legal-deadlines.test.ts
@src/__tests__/legal-prescription.test.ts
@src/__tests__/legal-waiver.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create parseFlexibleDate utility with tests</name>
  <files>src/lib/legal/parse-flexible-date.ts, src/__tests__/parse-flexible-date.test.ts, src/lib/legal/index.ts</files>
  <behavior>
    - parseFlexibleDate("2026-03-15") returns "2026-03-15" (ISO passthrough)
    - parseFlexibleDate("03/15/2026") returns "2026-03-15" (US MM/DD/YYYY)
    - parseFlexibleDate("March 15, 2026") returns "2026-03-15" (natural language month name)
    - parseFlexibleDate("march 15, 2026") returns "2026-03-15" (case-insensitive)
    - parseFlexibleDate("Mar 15, 2026") returns "2026-03-15" (abbreviated month)
    - parseFlexibleDate("15 March 2026") returns "2026-03-15" (day-first natural)
    - parseFlexibleDate("not-a-date") throws Error with descriptive message
    - parseFlexibleDate("") throws Error
    - parseFlexibleDate("2026-13-45") throws Error (invalid month/day)
    - parseFlexibleDate("02/29/2025") throws Error (not a leap year)
  </behavior>
  <action>
    1. Create src/lib/legal/parse-flexible-date.ts with a single exported function `parseFlexibleDate(input: string): string` that returns an ISO YYYY-MM-DD string. No external date libraries -- use regex matching and the Date constructor for validation:
       - Pattern 1 (ISO): /^\d{4}-\d{2}-\d{2}$/ -- validate via Date constructor, return as-is if valid
       - Pattern 2 (US): /^(\d{1,2})\/(\d{1,2})\/(\d{4})$/ -- extract MM, DD, YYYY, validate, return ISO
       - Pattern 3 (natural): /^(\w+)\s+(\d{1,2}),?\s+(\d{4})$/ -- month name lookup (full + abbreviated), extract day/year, validate, return ISO
       - Pattern 4 (day-first natural): /^(\d{1,2})\s+(\w+)\s+(\d{4})$/ -- same month lookup but day comes first
       - Validation: construct Date from extracted parts, verify getUTCFullYear/Month/Date match extracted values (catches Feb 30, etc.)
       - Throw Error("Invalid date: '<input>'. Expected ISO (YYYY-MM-DD), US (MM/DD/YYYY), or natural (Month DD, YYYY).") on failure
    2. Add `parseFlexibleDate` to the barrel export in src/lib/legal/index.ts
    3. Write tests in src/__tests__/parse-flexible-date.test.ts covering all behaviors listed above. Tests do NOT need fake timers since parseFlexibleDate is pure date parsing with no "today" dependency.
  </action>
  <verify>
    <automated>cd /Users/emelsonmaestro/Library/CloudStorage/GoogleDrive-emelsonmaestro@gmail.com/My\ Drive/WORK/ETM/Projects/taxspecialista-shop/.claude/worktrees/heuristic-wescoff && npx vitest run src/__tests__/parse-flexible-date.test.ts</automated>
  </verify>
  <done>parseFlexibleDate handles ISO, US, and natural language dates; throws on invalid input; all parse tests green</done>
</task>

<task type="auto">
  <name>Task 2: Integrate parseFlexibleDate into all three legal tools and update tool schema descriptions</name>
  <files>src/lib/legal/calculate-deadlines.ts, src/lib/legal/calculate-prescription.ts, src/lib/legal/check-waiver-validity.ts, src/lib/ai/tools.ts</files>
  <action>
    1. In calculate-deadlines.ts:
       - Import parseFlexibleDate from "./parse-flexible-date"
       - At the top of calculateDeadlines(), normalize all date inputs: `const loaReceiptDate = parseFlexibleDate(input.loaReceiptDate)` and same for optional dates (only if provided, e.g. `const loaIssuanceDate = input.loaIssuanceDate ? parseFlexibleDate(input.loaIssuanceDate) : undefined`)
       - Use the normalized local variables in all addDays/daysUntil calls instead of input.* directly
       - Do NOT change the function signature or return types

    2. In calculate-prescription.ts:
       - Import parseFlexibleDate from "./parse-flexible-date"
       - At the top of calculatePrescription(), normalize: `const assessmentBasisDate = parseFlexibleDate(input.assessmentBasisDate)` and `const taxPeriodEnd = parseFlexibleDate(input.taxPeriodEnd)`
       - Use normalized locals in addYears/daysUntil calls
       - Do NOT change the function signature or return types

    3. In check-waiver-validity.ts:
       - Import parseFlexibleDate from "./parse-flexible-date"
       - At the top of checkWaiverValidity(), normalize: `const waiverSignedDate = parseFlexibleDate(input.waiverSignedDate)`, `const waiverExpiryDate = input.waiverExpiryDate ? parseFlexibleDate(input.waiverExpiryDate) : null`, `const prescriptionExpiryAtSigning = parseFlexibleDate(input.prescriptionExpiryAtSigning)`
       - Use normalized locals in isDateAfter call and in the description string for SIGNED_AFTER_PRESCRIPTION_EXPIRED defect
       - Do NOT change the function signature or return types

    4. In src/lib/ai/tools.ts:
       - Update the .describe() strings for ALL date parameters across all three computation tools: change "ISO 8601 date" to "Date (ISO YYYY-MM-DD, MM/DD/YYYY, or 'Month DD, YYYY')" so the LLM knows it can pass flexible formats
       - Do NOT change the zod type (keep z.string()) -- only change the description text
  </action>
  <verify>
    <automated>cd /Users/emelsonmaestro/Library/CloudStorage/GoogleDrive-emelsonmaestro@gmail.com/My\ Drive/WORK/ETM/Projects/taxspecialista-shop/.claude/worktrees/heuristic-wescoff && npx vitest run src/__tests__/legal-deadlines.test.ts src/__tests__/legal-prescription.test.ts src/__tests__/legal-waiver.test.ts src/__tests__/parse-flexible-date.test.ts</automated>
  </verify>
  <done>All three legal tools normalize date inputs via parseFlexibleDate before computation; tool schema descriptions updated to advertise flexible formats; all 4 test files pass (existing tests unchanged + new parse tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM -> tool function | AI model passes date strings extracted from user conversation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T (Tampering) | parseFlexibleDate | mitigate | Validate parsed Date object matches extracted components (prevents Date constructor coercion of invalid dates like Feb 30 -> Mar 2) |
| T-quick-02 | D (DoS) | parseFlexibleDate | accept | Input comes only from LLM tool calls, not user-facing; string length is bounded by zod schema |
</threat_model>

<verification>
1. Run full legal test suite: `npx vitest run src/__tests__/legal-*.test.ts src/__tests__/parse-flexible-date.test.ts`
2. All existing tests pass without modification (ISO dates still work)
3. parseFlexibleDate tests cover ISO, US, natural language, and error cases
4. TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- parseFlexibleDate("2026-03-15"), parseFlexibleDate("03/15/2026"), and parseFlexibleDate("March 15, 2026") all return "2026-03-15"
- All three legal tools accept any supported format transparently
- Invalid dates throw clear errors
- All tests pass (existing + new)
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/quick/260413-e31-phase-3-deadline-and-legal-precision-too/260413-e31-SUMMARY.md`
</output>
